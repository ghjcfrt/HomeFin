# 家庭财务小管家系统架构图

版本：V1.0  
日期：2026-04-02

本文档给出项目的系统架构图，覆盖以下常用元素：层次、模块、组件、数据存储、服务器、接口箭头、外部系统、边界、网络、注释。

## 一、总体架构图（部署/网络视角）

```mermaid
flowchart LR
	%% ========== 边界：用户侧 ==========
	subgraph U[边界A：用户终端（Client Network）]
		User[家庭记账用户]
		Browser[Web 浏览器\nindex.html + app.js + styles.css + Chart.js]
		User --> Browser
	end

	%% ========== 边界：应用服务侧 ==========
	subgraph S[边界B：本地应用服务区（HomeFin Host）]
		direction TB

		subgraph FE[前端静态服务模块]
			StaticServer[静态文件服务器\npython -m http.server :5500]
		end

		subgraph BE[后端 API 服务器模块]
			APIServer[FastAPI 应用服务器\nuvicorn app.main:app :8000]
			Router1[组件：transactions_router]
			Router2[组件：imports_router]
			Router3[组件：stats_router]
			Router4[组件：system_router]
			Service1[组件：ocr_service]
			Service2[组件：alipay_import_service]
			Service3[组件：wechat_import_service]
			Crud[组件：db.crud + SQLAlchemy]

			APIServer --> Router1
			APIServer --> Router2
			APIServer --> Router3
			APIServer --> Router4
			Router2 --> Service1
			Router2 --> Service2
			Router2 --> Service3
			Router1 --> Crud
			Router2 --> Crud
			Router3 --> Crud
			Router4 --> Crud
		end

		subgraph DB[数据存储模块]
			SQLite[(SQLite\nhomefin.db)]
		end

		Crud --> SQLite
	end

	%% ========== 边界：外部系统 ==========
	subgraph E[边界C：外部系统/外部数据源]
		Alipay[支付宝账单导出系统\nCSV 文件]
		Wechat[微信账单导出系统\nXLSX 文件]
		OCRLib[RapidOCR 引擎\nONNX Runtime]
	end

	%% ========== 网络与接口箭头 ==========
	Browser -. 本地网络读取静态页面 :5500 .-> StaticServer
	Browser -- HTTP/JSON API :8000 --> APIServer

	Alipay -- 文件导出 --> Browser
	Wechat -- 文件导出 --> Browser
	Browser -- multipart/form-data 上传 --> APIServer

	Service1 --> OCRLib

	Note1[注释：当前部署模式为单机本地部署，前后端分离，
通过 localhost 端口通信。]
	Note1 -.说明.-> APIServer
```

## 二、逻辑分层架构图（层次/模块视角）

```mermaid
flowchart TB
	L1[表示层（Presentation Layer）\n- 表单录入\n- 导入预览编辑\n- 图表展示]
	L2[应用层（Application Layer）\n- 路由控制\n- 参数校验\n- 错误处理]
	L3[领域服务层（Domain Service Layer）\n- OCR 解析\n- 支付宝 CSV 解析\n- 微信 XLSX 解析\n- 分类推断与去重键生成]
	L4[数据访问层（Data Access Layer）\n- SQLAlchemy CRUD\n- 聚合统计\n- 幂等导入写入]
	L5[数据存储层（Data Storage Layer）\n- SQLite: transactions]

	L1 --> L2
	L2 --> L3
	L2 --> L4
	L4 --> L5
```

## 三、关键接口箭头（示例）

1. 浏览器 -> 后端：GET /transactions（查询记录）。
2. 浏览器 -> 后端：POST /transactions（新增记录）。
3. 浏览器 -> 后端：POST /transactions/batch（批量入库）。
4. 浏览器 -> 后端：POST /ocr/preview（OCR 预览）。
5. 浏览器 -> 后端：POST /imports/alipay/preview、POST /imports/alipay。
6. 浏览器 -> 后端：POST /imports/wechat/preview、POST /imports/wechat。
7. 浏览器 -> 后端：GET /stats/category/{txn_type}、GET /stats/monthly。

## 四、架构边界与网络说明

1. 网络边界：
   - Client Network：浏览器访问静态页面与 API。
   - Host Network：前后端服务与数据库在同一主机。
2. 可信边界：
   - 外部账单文件（CSV/XLSX）属于非可信输入，必须经过校验与清洗。
3. 数据边界：
   - SQLite 为唯一持久化存储，导入去重依赖 (source, import_key) 唯一约束。

## 五、检查点

1. 是否同时体现部署视角和逻辑分层视角。
2. 是否包含外部系统、服务器、数据库和接口箭头。
3. 是否标注边界与网络关系（端口、协议、传输方式）。
4. 是否体现核心扩展点（OCR、导入解析、统计聚合、幂等去重）。
