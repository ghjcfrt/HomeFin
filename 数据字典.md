# HomeFin 数据字典

版本：V0.2
日期：2026-04-29
适用范围：`backend/app/db/models.py` 定义的当前数据库结构

## 1. 数据库概览

HomeFin 当前使用本地 SQLite 数据库，数据库文件位于项目根目录的 `homefin.db`。

当前数据库包含 2 张业务表，共 14 个字段：

1. `transactions`：交易记录表，9 个字段。
2. `budgets`：预算表，5 个字段。

当前数据库没有显式外键关系，主要通过业务逻辑和唯一约束维护数据一致性。

## 2. 交易记录表 `transactions`

### 2.1 表说明

交易记录表用于保存手工录入、OCR 导入、支付宝导入和微信导入后的交易数据。

### 2.2 字段字典

| 字段名         | 数据类型                    | 长度/精度 | 是否可空 | 默认值         | 说明                                                                                                   |
| -------------- | --------------------------- | --------- | -------- | -------------- | ------------------------------------------------------------------------------------------------------ |
| `id`         | `Integer`                 | -         | 否       | 自增长         | 主键，交易记录唯一标识符。                                                                             |
| `type`       | `String`                  | 20        | 否       | 无             | 交易类型，取值为 `income` 或 `expense`。                                                           |
| `category`   | `String`                  | 50        | 否       | 无             | 交易分类名称。                                                                                         |
| `amount`     | `Float`                   | -         | 否       | 无             | 交易金额，业务上要求大于 0。                                                                           |
| `note`       | `String`                  | 200       | 是       | `NULL`       | 备注信息。                                                                                             |
| `txn_date`   | `Date`                    | -         | 否       | 无             | 交易日期。                                                                                             |
| `source`     | `String`                  | 30        | 是       | `NULL`       | 导入来源标识。当前代码实际使用的值主要有 `alipay_csv`、`wechat_xlsx`；手工录入或未标记数据可为空。 |
| `import_key` | `String`                  | 64        | 是       | `NULL`       | 导入幂等键，用于重复导入去重。                                                                         |
| `created_at` | `DateTime(timezone=True)` | -         | 否       | `func.now()` | 记录创建时间。                                                                                         |

### 2.3 索引与约束

1. 主键：`id`。
2. 普通索引：`source`、`import_key`。
3. 唯一索引：`ux_transactions_source_import_key`，对应 `(source, import_key)`。

### 2.4 业务说明

1. `type` 的值域来自交易类型枚举，当前仅允许 `income` 和 `expense`。
2. `category` 的合法值由业务层分类常量控制，收入和支出分类分别来自 `INCOME_CATEGORIES` 与 `EXPENSE_CATEGORIES`。
3. `source` 与 `import_key` 主要用于支付宝 CSV 和微信 XLSX 的幂等导入；普通手工新增记录通常不需要填写。
4. `created_at` 由数据库自动写入，不依赖前端传值。

## 3. 预算表 `budgets`

### 3.1 表说明

预算表用于保存每个月的预算配置，包括总预算和分类预算。系统通过同一张表管理月度总预算与分类预算。

### 3.2 字段字典

| 字段名         | 数据类型                    | 长度/精度 | 是否可空 | 默认值         | 说明                               |
| -------------- | --------------------------- | --------- | -------- | -------------- | ---------------------------------- |
| `id`         | `Integer`                 | -         | 否       | 自增长         | 主键，预算记录唯一标识符。         |
| `month`      | `String`                  | 7         | 否       | 无             | 预算所属月份，格式为 `YYYY-MM`。 |
| `category`   | `String`                  | 50        | 否       | 无             | 预算分类名称。                     |
| `amount`     | `Float`                   | -         | 否       | 无             | 预算金额，业务上要求大于 0。       |
| `created_at` | `DateTime(timezone=True)` | -         | 否       | `func.now()` | 记录创建时间。                     |

### 3.3 索引与约束

1. 主键：`id`。
2. 普通索引：`month`、`category`。
3. 唯一约束：`ux_budgets_month_category`，对应 `(month, category)`。

### 3.4 业务说明

1. `month` 固定采用 `YYYY-MM` 格式，例如 `2026-04`。
2. `category` 的正常取值与交易分类一致，来源于系统分类常量。
3. `category` 还存在一个特殊隐藏值 `__TOTAL__`，用于保存当月总预算。
4. 当某个月不配置总预算时，对应的 `__TOTAL__` 记录可以不存在。

## 4. 业务值域说明

### 4.1 交易类型 `type`

| 值          | 含义 |
| ----------- | ---- |
| `income`  | 收入 |
| `expense` | 支出 |

### 4.2 收入分类 `category`

| 值           | 含义           |
| ------------ | -------------- |
| `工资`     | 工资收入       |
| `奖金`     | 奖金收入       |
| `兼职`     | 兼职收入       |
| `理财收益` | 理财相关收益   |
| `红包`     | 红包收入       |
| `其他收入` | 其他未归类收入 |

### 4.3 支出分类 `category`

| 值           | 含义                 |
| ------------ | -------------------- |
| `餐饮`     | 餐饮消费             |
| `交通`     | 交通出行             |
| `购物`     | 购物消费             |
| `日用`     | 日常用品             |
| `房租`     | 房租支出             |
| `水电煤`   | 水电煤及物业相关支出 |
| `通讯`     | 通讯费用             |
| `娱乐`     | 娱乐消费             |
| `医疗`     | 医疗支出             |
| `教育`     | 教育培训             |
| `旅行`     | 旅行相关支出         |
| `其他支出` | 其他未归类支出       |

### 4.4 预算特殊分类 `category`

| 值            | 含义                   |
| ------------- | ---------------------- |
| `__TOTAL__` | 月度总预算的隐藏存储项 |

## 5. 维护说明

1. 本数据字典按当前代码结构整理，后续如果新增字段、表或约束，应同步更新本文件。
2. 如果数据库迁移到其他引擎或增加外键关系，应补充物理存储类型和关联说明。
3. 如果分类常量发生变化，应同步更新“业务值域说明”。
